//arms exercises

//bicep curls
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-1").style.display = "none";
    }
});

card1.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-1").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-1").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//hammer curls
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-2").style.display = "none";
    }
});

hammerCurls.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-2").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-2").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//preacher curls
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-3").style.display = "none";
    }
});

preacherCurls.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-3").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-3").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//tricep pushdown
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-4").style.display = "none";
    }
});

tricepPushdown.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-4").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-4").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//tricep rope pushdown
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-5").style.display = "none";
    }
});

tricepRopePushdown.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-5").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-5").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//bench tricep dips
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-6").style.display = "none";
    }
});

benchTricepDips.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-6").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-6").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

//backward wrist curls
document.addEventListener("DOMContentLoaded", function() {
    // Check if the page is being refreshed
    if (performance.navigation.type === 1) {
        // Hide the .chestPress div
        document.querySelector(".instruction-7").style.display = "none";
    }
});

backwardWristCurls.addEventListener("click", async () => {
    // Show right2 and hide right1
    document.querySelector(".instruction-7").style.display = "block";
    document.querySelector(".gallery-area").style.display = "none";

    // Scroll to a specific element
    document.querySelector(".instruction-7").scrollIntoView({ 
        behavior: 'smooth' 
    });
});

backBtn1.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-1").style.display = "none";
})

backBtn2.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-2").style.display = "none";
})

backBtn3.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-3").style.display = "none";
})

backBtn4.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-4").style.display = "none";
})

backBtn5.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-5").style.display = "none";
})

backBtn6.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-6").style.display = "none";
})

backBtn7.addEventListener("click", async () => {
    // Show right2 and hide right1
        document.querySelector(".gallery-area").style.display = "block";
        document.querySelector(".gallery-area").scrollIntoView({ 
            behavior: 'smooth' 
        });
        
        document.querySelector(".instruction-7").style.display = "none";
})










